# Node
node_modules/
dist/
coverage/
*.log
.env
.env.*
!.env.example

# Go — compiled service binaries (each main package builds to a binary named
# after its directory; never commit these).
/services/mpc-signer/mpc-signer
/services/policy-service/policy-service
/services/temporal-worker/temporal-worker
/services/temporal-worker/worker
*.test

# Editor / OS
.DS_Store
.idea/
.vscode/

# SOC 2 evidence packs: dated operational snapshots, produced by
# scripts/collect-soc2-evidence.sh. They contain live configuration and row
# counts and belong in access-controlled storage, not in the repository --
# but the collector is checked in so the process is reviewable.
evidence/
